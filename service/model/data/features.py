"""
特徵工程：從清洗後的 DataFrame 衍生特徵欄位、categorical encoding。
CIC IDS 2019 資料集不用先空著
"""
import pandas as pd


def build_features(df: pd.DataFrame) -> pd.DataFrame:
    pass


def _encode_proto(df: pd.DataFrame) -> pd.DataFrame:
    pass


def _encode_service(df: pd.DataFrame) -> pd.DataFrame:
    pass


def _derive_ratio_features(df: pd.DataFrame) -> pd.DataFrame:
    pass


def _derive_throughput_features(df: pd.DataFrame) -> pd.DataFrame:
    pass
