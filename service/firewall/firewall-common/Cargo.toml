[package]
name = "firewall-common"
version = "0.1.0"
edition = "2021"

[dependencies]
aya-ebpf = { version = "0.1.1", optional = true }
aya = { version = "0.13.1", optional = true }

[build-dependencies]
toml = "1.0"

[features]
user = ["dep:aya"]
