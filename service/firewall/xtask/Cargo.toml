[package]
name = "xtask"
version = "0.1.0"
edition = "2021"

[dependencies]
# 在這裡，為你的 xtask 工具，添加它需要的依賴
# 例如，用於解析命令列參數的 clap
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
tokio = { version = "1", features = ["full"] }